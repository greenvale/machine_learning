# Machine Learning projects

## C++ work

The `compuational_graph` class contains operation nodes for additional, multiplication, etc. for scalar, Matrix and eventually Tensor operations to construct computational graphs.
In the future an optimisation function will be written to allow neural networks to be set up and trained. I have already made a neural network code using my Matrix library, this is available in the mllib.

The `tensor` class is my version of an arbitrary dimension tensor.

## Python work

I will upload my python tensorflow projects on here too as and when they are completed.
